import { NotFoundContent, MainLayout } from '../components';

const NotFound = () => {
  return (
    <MainLayout>
      <NotFoundContent />
    </MainLayout>
  );
};

export default NotFound;
